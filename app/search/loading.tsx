// Auto-rendered as the Suspense fallback for /search during navigation.
// Plain text now; Phase 4 will swap in a results-grid skeleton.
export default function Loading() {
  return <p>Loading search...</p>;
}
