// Auto-rendered as the Suspense fallback for /cart during navigation.
// Plain text now; Phase 6 will swap in a cart-row skeleton.
export default function Loading() {
  return <p>Loading cart...</p>;
}
